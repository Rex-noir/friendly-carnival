export enum UserActions {
    UPDATE,
    DELETE,
    BAN,
}
export enum UserStatus {
    ACTIVE = "active",
    BANNED = "banned",
}
export enum UserRoles {
    ADMIN = "admin",
    USER = "user",
    AUTHOR = "author",
}
